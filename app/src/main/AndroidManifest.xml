<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.final_adminside">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:largeHeap="true"
        android:hardwareAccelerated="false">
        <activity android:name=".eventrecycler" android:parentActivityName=".cardmainmenu" />
        <activity android:name=".projectrecycler" android:parentActivityName=".cardmainmenu" />
        <activity android:name=".Event_upload_image" android:parentActivityName=".add_event" />
        <activity android:name=".add_event" android:parentActivityName=".cardmainmenu" />
        <activity android:name=".Project_upload_image" android:parentActivityName=".create_project" />
        <activity android:name=".create_project" android:parentActivityName=".cardmainmenu" />
        <activity android:name=".cardmainmenu" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>